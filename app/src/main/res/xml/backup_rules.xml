<?xml version="1.0" encoding="utf-8"?>

<!--
This XML file defines which app data should be included or excluded
from full backup (e.g., via Google Auto Backup or ADB).

Used by the `android:fullBackupContent` attribute in the manifest.

Include only necessary data (e.g. user settings) and exclude sensitive
or dynamically generated files to protect user privacy and reduce backup size.

   Sample backup rules file; uncomment and customize as necessary.
   See https://developer.android.com/guide/topics/data/autobackup
   for details.
   Note: This file is ignored for devices older than API 31
   See https://developer.android.com/about/versions/12/backup-restore
-->
<full-backup-content>
    <!--
   <include domain="sharedpref" path="."/>
   <exclude domain="sharedpref" path="device.xml"/>
-->
</full-backup-content>